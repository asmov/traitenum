[package]
name = "traitenum-test-importer"
version = "0.1.0"
edition = "2021"

[dependencies]
traitenum-test-exporter-traits = { version = "0.1.0", path = "../exporter/traits" }
traitenum-test-exporter-macro = { version = "0.1.0", path = "../exporter/macro" }
