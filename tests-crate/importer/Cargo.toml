[package]
name = "traitenum-testcrate-importer"
version = "0.1.0"
edition = "2021"

[dependencies]
traitenum = { version = "0.1.0", path = "../.." }
traitenum-testcrate-exporter = { version = "0.1.0", path = "../exporter" }
